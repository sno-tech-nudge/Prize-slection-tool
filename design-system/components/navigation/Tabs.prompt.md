Underlined tab bar; active tab carries a red underline.

```jsx
<Tabs
  defaultValue="about"
  items={[{id:'about',label:'about'},{id:'programs',label:'programs'},{id:'apply',label:'apply'}]}
  onChange={setTab}
/>
```
