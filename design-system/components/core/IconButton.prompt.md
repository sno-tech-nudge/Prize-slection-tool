Square icon-only button. Pass one icon element as children and an accessible `label`.

```jsx
<IconButton label="search"><SearchIcon /></IconButton>
<IconButton variant="primary" label="add"><PlusIcon /></IconButton>
```

Variants: `primary | secondary | ghost`. Sizes: `sm | md | lg`.
