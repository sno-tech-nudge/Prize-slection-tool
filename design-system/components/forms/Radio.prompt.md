Radio group with square indicators (no circles — brand rule).

```jsx
<Radio
  name="stage"
  defaultValue="idea"
  options={[{value:'idea',label:'idea'},{value:'early',label:'early stage'},{value:'scaling',label:'scaling'}]}
/>
```
