Text input with label and helper/error. Red focus ring, square edges.

```jsx
<Input label="email" placeholder="you@org.in" helper="we'll never share it" />
<Input label="name" error="required" />
```
